Read me file:


1. code works on all the worksheets one at a time.
2.solution works even if the data is not sorted
3. the code runs in a short time
4. the module4 is the final macro code to be run.  rest of them are just separate work done on columns to test
5. module 1 is vba code for getting uniquw ticker values 
6. module 2 converts the date column into date format.
